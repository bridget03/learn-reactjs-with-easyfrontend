import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

header.propTypes = {
    
};

function header(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default header;